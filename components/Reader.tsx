
import React from 'react';

// Reader component has been removed as per user request to use FeedPost view instead.
// Keeping file to prevent import errors in any potential legacy code paths.
export const Reader: React.FC<any> = () => {
  return null;
};
